import 'package:flutter/material.dart';
import 'initial_data_loader.dart';

class HomeScreen extends StatelessWidget {
  const HomeScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const InitialDataLoader();
  }
}